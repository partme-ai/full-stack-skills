> **标准说明**：以下为 OctoPanel 实例全文（`partme-docs/1、OctoPanel/1、…`），`{Name}` 为占位符。

---

# {Name} 命名与品牌说明

> 文件名保留历史名，仅用于兼容旧引用。正文中的正式产品名统一为 **{Name}**。

## 1. 品牌迁移结论

- **正式产品名**：`{Name}`
- **产品定位**：`多引擎智能体控制平面`
- **一句话定义**：`面向 OpenClaw / ZeroClaw / OctoClaw 的安装、纳管、配置、运维、扩展与审计平台`
- `{Name}` 退为历史名、早期代号、旧文档兼容说明

## 2. 为什么从 {Name} 迁移到 {Name}

`{Name}` 更像早期辅助工具名，而 `{Name}` 更适合作为：

- 多引擎统一控制平面品牌
- Web 主导的控制台品牌
- 后续承载实例、节点、任务、扩展、审计等平台能力的母品牌

## 3. 产品与引擎边界

### 3.1 平台

`{Name}` 是平台，不是引擎。

负责：

- 安装与预检
- 纳管与配置
- 运维与任务
- 扩展与审计

### 3.2 被管理引擎

| 类型 | 名称 | 角色 |
|------|------|------|
| 引擎 | `OpenClaw` | 被 {Name} 安装、纳管与运维 |
| 引擎 | `ZeroClaw` | 被 {Name} 安装、纳管与运维 |
| 引擎 | `OctoClaw` | 被 {Name} 安装、纳管与运维 |

## 4. 交付形态命名

| 形态 | 标准名 | 定位 |
|------|--------|------|
| Web 控制台 | `{Name} Web` | 团队控制平面，更像 1Panel 的企业形态 |
| 桌面控制壳 | `{Name} Desktop` | 本地桌面壳，管理本机和远程实例 |
| 同机执行进程 | `{Name} Node` | Node.js 独立进程，负责安装、预检、配置生成、服务管理、日志采集、健康检查 |

## 5. 对外表达规范

- 首次出现：`{Name}（多引擎智能体控制平面）`
- 历史兼容说明：`{Name}（原 {Name}）`
- 不再使用 `{Name}` 作为当前主产品名

## 6. 品牌关系说明

- `1Panel`：借鉴信息架构与控制台范式
- `clawpanel`：借鉴安装 / 诊断 / 修复主链、三形态与多实例代理思路
- `{Name}`：在两者启发上形成自己的平台品牌与产品边界
